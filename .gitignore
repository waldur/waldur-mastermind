/dist
/build
/venv
*.egg
*.egg-info
*.pyc
*.mo
.DS_Store
src/waldur_core/server/settings.py
src/waldur_core/server/settings_local.py

# Editor artifacts
/.idea
/.project
/.pydevproject

# llm
.claude/settings.local.json

# Testing artifacts

TEST-*.xml
coverage.xml
test_report.xml
.cache/
.pytest_cache/
pytest.ini

/static
__pycache__/

.vscode/
pyrightconfig.json

# Local Matrix appservice registration (contains dev tokens)
docker/matrix-dev/appservices/

# Local scratch and generated artifacts (never commit to this repo)
scratchpad/
waldur-typescript-sdk/
waldur-openapi-schema.yaml

# Transient changelog fragments written by waldur-docs/scripts/release.sh,
# always cleared and rewritten on each run — assemble_changelog consumes and
# clears this directory itself.
changelog/next/

# Release files assembled by the same script. They are published from
# waldur-docs (docs/changelog/releases/), which keeps their history.
changelog/releases/

# UI review screenshot artifacts (not for commit)
.ui-review-shots/
