# Generated by Django 6.0.8 on 2026-09-18 09:23

import django_fsm
from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("marketplace", "0294_offering_merge"),
    ]

    operations = [
        migrations.AddField(
            model_name="offeringmerge",
            name="progress",
            field=models.JSONField(
                blank=True,
                default=dict,
                help_text="Execution progress: the current step, steps and rows done and in total. Committed outside the merge's transaction while it runs.",
            ),
        ),
        migrations.AlterField(
            model_name="offeringmerge",
            name="state",
            field=django_fsm.FSMField(
                choices=[
                    ("draft", "Draft"),
                    ("previewed", "Previewed"),
                    ("queued", "Queued"),
                    ("running", "Running"),
                    ("done", "Done"),
                    ("failed", "Failed"),
                    ("undoing", "Undoing"),
                    ("undone", "Undone"),
                ],
                default="draft",
                max_length=20,
            ),
        ),
    ]
