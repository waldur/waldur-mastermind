# Generated by Django 2.2.13 on 2021-02-11 12:06

from django.core.exceptions import ObjectDoesNotExist
from django.db import migrations, models


def set_default_flags_to_categories(apps, schema_editor):
    Category = apps.get_model('marketplace', 'Category')

    try:
        vm_category = Category.objects.get(title='VMs')
    except ObjectDoesNotExist:
        pass
    else:
        vm_category.default_vm_category = True
        vm_category.save()

    try:
        storage_category = Category.objects.get(title='Storage')
    except ObjectDoesNotExist:
        pass
    else:
        storage_category.default_volume_category = True
        storage_category.save()


class Migration(migrations.Migration):

    dependencies = [
        ('marketplace', '0041_drop_package'),
    ]

    operations = [
        migrations.AddField(
            model_name='category',
            name='default_vm_category',
            field=models.BooleanField(
                default=False,
                help_text='Set to "true" if this category is for OpenStack VM. Only one category can have "true" value.',
            ),
        ),
        migrations.AddField(
            model_name='category',
            name='default_volume_category',
            field=models.BooleanField(
                default=False,
                help_text='Set to true if this category is for OpenStack Volume. Only one category can have "true" value.',
            ),
        ),
        migrations.RunPython(set_default_flags_to_categories),
    ]
