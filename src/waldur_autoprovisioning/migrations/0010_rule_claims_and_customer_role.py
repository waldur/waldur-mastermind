# Generated by Django 6.0.8 on 2026-09-08 21:39

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [
        ("permissions", "0028_userrole_source"),
        ("waldur_autoprovisioning", "0001_squashed_0009"),
    ]

    operations = [
        migrations.AddField(
            model_name="rule",
            name="create_project",
            field=models.BooleanField(
                default=True,
                help_text="Create (or join) a project for the matched user. Disable to grant only the organization-level role.",
            ),
        ),
        migrations.AddField(
            model_name="rule",
            name="customer_role",
            field=models.ForeignKey(
                blank=True,
                help_text="Role granted on the organization itself. Leave empty to grant no organization-level role.",
                null=True,
                on_delete=django.db.models.deletion.CASCADE,
                related_name="autoprovisioning_customer_rules",
                to="permissions.role",
            ),
        ),
        migrations.AddField(
            model_name="rule",
            name="revoke_when_unmatched",
            field=models.BooleanField(
                default=False,
                help_text="Revoke the roles this rule granted once the user stops matching it. Off by default so enabling a rule cannot silently strip access that is already in use.",
            ),
        ),
        migrations.AddField(
            model_name="rule",
            name="user_claims",
            field=models.JSONField(
                blank=True,
                default=dict,
                help_text='Identity provider claims the user must carry, as {"claim": ["accepted", "values"]}. All claims must match; within one claim any value matches. A value ending in \'*\' matches by prefix.',
            ),
        ),
    ]
