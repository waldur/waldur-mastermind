{
    admin off
    # Bind both IPv4 and IPv6 so lk-jwt-service, inside the shared network
    # namespace, reaches the proxy on whichever loopback address it dials.
    default_bind 0.0.0.0 ::
}

localhost:8448 {
    tls internal
    reverse_proxy 127.0.0.1:6167
}
